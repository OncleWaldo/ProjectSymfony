{% extends 'base.html.twig' %}

{% block title %}CategorySalle{% endblock %}

{% block body %}
    <h1>CategorySalle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_salle.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_salle.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_salle_index') }}">back to list</a>

    <a href="{{ path('category_salle_edit', {'id': category_salle.id}) }}">edit</a>

    {{ include('category_salle/_delete_form.html.twig') }}
{% endblock %}
